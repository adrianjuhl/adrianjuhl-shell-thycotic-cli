

#### Running a subset of tests

For example, to execute only the tests in the 'Describe "get_json_element_value"' block, use:
```
shellspec --example="get_json_element_value"  --kcov --format=documentation
```

